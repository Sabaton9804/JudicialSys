/**
 * Modo shell reducido (rama `solo-radicacion-tutelas`).
 * Solo se muestran Radicación y Tutelas en la UI principal.
 *
 * En `main` este archivo no existe o exporta `false`; no fusionar esta rama
 * sin revisar si querés volver al panel completo.
 */
export const SHELL_SOLO_RADICACION_TUTELAS = true
